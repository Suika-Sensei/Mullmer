// App-wide constants: camera capture defaults, image MIME types,
// localStorage keys, and theme names.
export const CAMERA_CONFIG = {
  VIDEO_WIDTH: 640,
  VIDEO_HEIGHT: 480,
};

export const IMAGE_FORMATS = {
  PNG: "image/png",
  JPEG: "image/jpeg",
};

export const STORAGE_KEYS = {
  THEME: "theme",
};

export const THEMES = {
  LIGHT: "light",
  DARK: "dark",
};
